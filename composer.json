{
    "name": "monkeyscloud/monkeyslegion-apex",
    "description": "AI Orchestration Engine for PHP 8.4 — Multi-model routing, pipelines, guardrails, structured output, and agents in one package.",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "MonkeysCloud",
            "email": "jorge@monkeys.cloud"
        }
    ],
    "require": {
        "php": "^8.4",
        "ext-curl": "*",
        "ext-json": "*",
        "ext-mbstring": "*",
        "psr/simple-cache": "^3.0",
        "psr/log": "^3.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "suggest": {
        "monkeyscloud/monkeyslegion-cache": "^2.0 — Required for semantic caching middleware",
        "monkeyscloud/monkeyslegion-cli": "Required for ai:chat and ai:costs console commands",
        "monkeyscloud/monkeyslegion-telemetry": "^2.0 — Required for distributed tracing, metrics, and structured logging",
        "ext-pcntl": "Required for parallel tool execution",
        "ext-sockets": "Required for parallel tool execution IPC"
    },
    "autoload": {
        "psr-4": {
            "MonkeysLegion\\Apex\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "MonkeysLegion\\Apex\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit"
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
